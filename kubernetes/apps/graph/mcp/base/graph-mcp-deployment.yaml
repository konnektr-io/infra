apiVersion: apps/v1
kind: Deployment
metadata:
  name: graph-mcp
  labels:
    app: graph-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graph-mcp
  template:
    metadata:
      labels:
        app: graph-mcp
    spec:
      nodeSelector:
        cloud.google.com/gke-spot: "true"
      containers:
        - name: backend
          image: ghcr.io/konnektr-io/graph-mcp:0.0.26
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: oauth-storage
              mountPath: /var/lib/fastmcp/oauth
          env:
            - name: AUTH0_DOMAIN
              value: "auth.konnektr.io"
            - name: AUTH0_AUDIENCE
              value: "https://graph.konnektr.io"
            - name: AUTH0_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: auth0-credentials
                  key: clientId
            - name: AUTH0_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: auth0-credentials
                  key: clientSecret
            - name: AUTH_ENABLED
              value: "true"
            - name: API_BASE_URL_TEMPLATE
              value: "https://{resource_id}.api.graph.konnektr.io"
            - name: API_TIMEOUT_SECONDS
              value: "30"
            - name: MCP_RESOURCE_URL
              value: "https://mcp.graph.konnektr.io"
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: EMBEDDING_ENABLED
              value: "true"
            - name: GOOGLE_GENAI_USE_VERTEXAI
              value: "true"
            - name: GOOGLE_CLOUD_PROJECT
              value: konnektr
            - name: GOOGLE_CLOUD_LOCATION
              value: europe-west1
            - name: GOOGLE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: mcp-vertexai
                  key: apikey
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
      volumes:
        - name: oauth-storage
          persistentVolumeClaim:
            claimName: graph-mcp-oauth-pvc
