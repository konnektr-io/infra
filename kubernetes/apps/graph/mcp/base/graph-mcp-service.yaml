apiVersion: v1
kind: Service
metadata:
  name: graph-mcp
  labels:
    app: graph-mcp
spec:
  selector:
    app: graph-mcp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
